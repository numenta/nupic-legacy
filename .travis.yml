branches:
  only:
    - dev-master
language: python
compiler:
  - gcc
python:
  - "2.6"
env:
  - NUPIC=$TRAVIS_BUILD_DIR
# Change this to your needs
before_script:
  - "source $NUPIC/env.sh"
script: "$NUPIC/build.sh"
