branches:
  only:
    - master
language: python
compiler:
  - gcc
python:
  - "2.6"
env:
  - INSTALL_DIR=$HOME/nta/eng NUPIC=$TRAVIS_BUILD_DIR BUILDDIR=$HOME/ntabuild DYLD_LIBRARY_PATH=$INSTALL_DIR/lib:$DYLD_LIBRARY_PATH LD_LIBRARY_PATH=$INSTALL_DIR/lib:$LD_LIBRARY_PATH
# Change this to your needs
before_script:
  - "source $NUPIC/env.sh"
script: "$NUPIC/build.sh"
